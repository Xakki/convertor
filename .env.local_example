## Шаблон .env.local (gitignored). Только ДИФФ к .env: секреты и хост-специфика.
## Порядок наложения: .env → .env.local → .env.test (при `make test*`).

MARIADB_PASSWORD=CHANGE_ME
MARIADB_ROOT_PASSWORD=CHANGE_ME
EXPORTER_PASS=CHANGE_ME
S3_SECRET=CHANGE_ME

# Общие для php / ws-gateway / воркеров (compose инжектит их отсюда во все три —
# это ЕДИНЫЙ источник, в app-symfony/.env.local их дублировать НЕ надо).
WORKER_API_TOKEN=CHANGE_ME
GATEWAY_INTERNAL_TOKEN=CHANGE_ME

# Harbor (make harbor-login) — DOCKER_REGISTRY в .env; здесь только секреты
DOCKER_USER=
DOCKER_PASS=

# ============================================================================
# REMOTE worker-хост (только воркеры + fluent, без серверной части) — отдельный
# шаблон: .env.local_worker_example. Не смешивать с этим файлом.
# ============================================================================
