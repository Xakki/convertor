# Тест-зависимости воркеров — единый источник истины.
# Вшиваются в отдельный :test-stage образ поверх runtime (docker/workers/test.Dockerfile).
# Никогда не попадают в production (:latest).
#
# Пинованные версии: зеркалят то, что Makefile-таргеты ставили руками.
# Плавающие (pytest, fastapi, …): floor-bound, совместимый с runtime-образами.
pytest>=8.0
pytest-asyncio>=0.23

# gateway-тесты в worker-data: нужен redis (не в data runtime-deps)
redis>=5.0

# e2e-тесты: test runner загружает/скачивает объекты напрямую (worker больше не
# работает с S3 — boto3 убран из runtime, но нужен для тест-рантайма).
boto3>=1.34

# WS/HTTP — пинованы зеркально к requirements-gateway.txt / per-worker requirements
websockets==13.1
httpx==0.27.2

# AI-тесты в worker-ffmpeg (fastapi, uvicorn, multipart, av, webrtcvad, numpy)
# av/webrtcvad-wheels поставляются как manylinux-wheels, системные libs не нужны.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.12
webrtcvad-wheels
av==17.1.0
numpy

# e2e DB seeding: raw SQL to MariaDB (INSERT user/file_storage/conversion + poll status=completed).
PyMySQL>=1.1
