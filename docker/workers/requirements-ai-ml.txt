# AI worker HEAVY ML deps — shared by both CPU and CUDA flavors.
# torch is NOT here: each flavor installs it from the correct wheel index
# (CPU: https://download.pytorch.org/whl/cpu;
#  CUDA: https://download.pytorch.org/whl/cu128).
#
# STT
faster-whisper==1.2.1
ctranslate2==4.8.0
av==17.1.0
# TTS (Python engine; espeak-ng binary installed via apt in each Dockerfile)
pyttsx3==2.99
# Embedding
sentence-transformers==5.6.0
# Audio quality evaluation
jiwer==4.0.0
# Monitoring
psutil
# ONNX runtime (ctranslate2 CPU backend)
onnxruntime==1.27.0
