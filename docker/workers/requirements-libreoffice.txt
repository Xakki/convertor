# worker-libreoffice pinned requirements (generated 2026-06-21)
# WS-transport: httpx (HTTP input download via gateway), websockets (WS client)
# Conversions shell out to soffice/pandoc/pdftotext; structlog for logging.
# python-docx (CNV-98): orientation on intermediate .docx before PDF export —
# soffice's writer_pdf_Export FilterData has no orientation property, so the
# worker sets page_width/page_height on the .docx itself; pinned to the same
# version as worker-image (requirements-image.txt, OCR→docx path).
httpx==0.27.2
websockets==13.1
jmespath==1.1.0
lxml==6.1.1
python-dateutil==2.9.0.post0
python-docx==1.2.0
six==1.17.0
structlog==26.1.0
typing_extensions==4.15.0
urllib3==2.7.0
