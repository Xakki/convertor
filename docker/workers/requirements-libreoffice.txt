# worker-libreoffice pinned requirements (generated 2026-06-21)
# Conversions shell out to soffice/pandoc/pdftotext, so no Python conversion
# libs are needed — only the queue + S3 transport (redis + boto3) and logging.
boto3==1.43.34
botocore==1.43.34
jmespath==1.1.0
python-dateutil==2.9.0.post0
redis==8.0.0
s3transfer==0.19.0
six==1.17.0
structlog==26.1.0
typing_extensions==4.15.0
urllib3==2.7.0
