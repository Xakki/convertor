# Convertor Service

Пример конвертации **Markdown → HTML** через реальный пайплайн.

- Категория: разметка (markup)
- Движок: Pandoc / LibreOffice
- Результат — живой пример на лендинге

> Загружать свой файл не нужно: этот результат сгенерирован заранее
> консольной командой `app:examples:seed`.
